services:
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: myapp-php
    working_dir: /var/www/html
    volumes:
      - ./app:/var/www/html
    entrypoint: ["/usr/local/bin/entrypoint.sh"]
    environment:
      APP_ENV: dev
      DATABASE_URL_MAIN: "postgresql://postgres:postgres@postgres:5432/main_db?serverVersion=16&charset=utf8"
      DATABASE_URL_TENANT_BASE: "postgresql://postgres:postgres@postgres:5432/placeholder?serverVersion=16&charset=utf8"
      MAILER_DSN: "smtp://mailhog:1025"
      MESSENGER_TRANSPORT_DSN: "doctrine://main?auto_setup=0"
    depends_on:
      - redis
      - postgres
      - mailhog

  nginx:
    image: nginx:1.27-alpine
    container_name: myapp-nginx
    ports:
      - "8080:80"
    volumes:
      - ./app/public:/var/www/html/public
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

  redis:
    image: redis:7-alpine
    container_name: myapp-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]

  postgres:
    image: postgres:16-alpine
    container_name: myapp-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: main_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  mailhog:
    image: mailhog/mailhog
    container_name: myapp-mailhog
    ports:
      - "8025:8025"
      - "1025:1025"

networks:
  default:
    name: myapp-network

volumes:
  postgres_data:
